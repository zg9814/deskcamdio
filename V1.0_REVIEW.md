# DeskCamdio v1.0 发布审查报告

审查日期：2026-08-23  
审查目录：`E:\deskcamdio\v1.0`

## 结论

当前版本不具备正式发布为 v1.0 的质量，建议暂定为 Alpha/0.5。

现有自动测试和覆盖率表现较好，但真实安装、启动、输入路由、相机、GBA、语音和资源释放链路仍存在阻断性问题。测试通过只能证明当前被覆盖的局部逻辑可以工作，不能证明构建产物能够在树莓派上正常部署和运行。

## 关键问题

### P0：Wheel 安装后无法加载应用

`AppManager` 依赖各应用目录内的 `app.toml` 加载应用描述，但构建出的 Wheel 中没有任何 `app.toml`，字体、声音等资源也没有打包。

安装该 Wheel 后，应用描述表为空，运行时进入待机应用时会发生 `KeyError`，设备无法正常启动。

相关位置：

- `src/deskcamdio/core/app_manager.py:41`
- `pyproject.toml:34`

### P0：待机页和桌面收不到触控输入

运行时只在 `RunState.APP` 状态下向当前应用转发 pygame 输入事件，没有在 `STANDBY` 和 `LAUNCHER` 状态转发输入。

由此会产生以下结果：

- 开机进入小鱼待机页后，点击无法进入桌面。
- 即使通过其他方式进入桌面，桌面图标也无法点击。
- 单元测试直接调用应用的 `handle_input()`，因此没有发现运行时路由问题。

相关位置：

- `src/deskcamdio/core/runtime.py:302`

### P0：部署流程不可用

目前发布和安装脚本存在多项阻断问题：

- `scripts/deploy_pi.ps1` 有 9 个 PowerShell 解析错误。
- `install_release.sh` 设置 `DESKCAMDIO_SELFTEST=1`，但设备入口只识别 `--selftest` 参数。
- 安装自检会启动完整运行时并持续等待，导致安装流程卡住。
- systemd 服务没有设置 `DESKCAMDIO_PLATFORM=raspberry_pi`。
- 未提供 `/etc/deskcamdio/device.env` 时，相机会默认使用模拟实现。
- 即使脚本修复，当前 Wheel 仍然缺少应用清单和资源。

相关位置：

- `scripts/install_release.sh:22`
- `scripts/deploy_pi.ps1`
- `src/deskcamdio/cli/device.py:36`
- `deploy/systemd/deskcamdio.service:12`
- `src/deskcamdio/platform/__init__.py:11`

### P1：相机页面尚未真正完成

相机服务具备预览接口，但相机应用没有调用 `preview_async()`，所以预览 Surface 不会被更新，页面会一直停留在预热或占位状态。

其他问题包括：

- 可见快门按钮的命中键是 `shutter`，输入处理没有对应分支，点击按钮不会拍照。
- 当前只有点击非控件预览区域才会触发拍照。
- 文件名精度只有秒，同一秒内连续拍摄可能覆盖上一张照片。
- 相机 socket 使用 `context.data_dir / "run"`，没有使用统一的 runtime `run_dir`。
- 拍照任务通过裸 `asyncio.create_task()` 创建，没有纳入应用资源生命周期。

相关位置：

- `src/deskcamdio/apps/camera/app.py:42`
- `src/deskcamdio/apps/camera/app.py:58`
- `src/deskcamdio/services/camera_client.py:64`

### P1：GBA 功能目前只是界面占位

点击 ROM 时只发布 `gba.launch_requested` 事件，源码内没有订阅该事件的处理器，也没有创建或启动 `GameSession`。

其他问题包括：

- ROM 索引调用不存在的 `StateStore.upsert_sync()`。
- StateStore 实际只提供异步 `upsert_rom()`。
- mGBA 的 `-s` 参数被错误地当作存档目录；该参数实际表示 frameskip。
- 没有完成 480×320 显示缩放和外部游戏状态切换。
- ROM Header checksum 算法不符合 GBA 校验规则，可能拒绝正常 ROM。
- 手柄监听线程的停止标记没有进入实际读取循环，可能无法正常退出。

相关位置：

- `src/deskcamdio/apps/gba/app.py:94`
- `src/deskcamdio/services/rom_library.py:72`
- `src/deskcamdio/services/state_store.py:287`
- `src/deskcamdio/services/game_session.py:62`

### P1：TaskScope 没有真正管理应用资源

`AppManager` 会为应用创建 `TaskScope`，但没有把 Scope 传入 `RuntimeContext`，应用无法向 Scope 注册自身任务、订阅、线程或子进程。

相机、设置、备忘录、番茄钟等应用仍然直接创建裸异步任务。相册应用还存在以下问题：

- 订阅 `photo.created` 后没有保存和调用取消订阅函数。
- 缩略图线程没有停止或 join 机制。
- dispose 只清理部分内存对象，不能保证后台资源已退出。

反复进入应用可能积累任务、事件订阅和线程，违背低内存重构的核心目标。

相关位置：

- `src/deskcamdio/core/app_manager.py:120`
- `src/deskcamdio/core/task_scope.py`
- `src/deskcamdio/apps/gallery/app.py:86`
- `src/deskcamdio/apps/gallery/app.py:166`

### P1：语音助手没有接入运行时

项目内没有发现创建 `VoiceService`、响应普通页面短按按键或调用完整语音交互流程的入口。

现有语音代码与规划仍存在明显差异：

- 本地 ASR 在线程中直接加载模型，不是按轮次启动的独立子进程。
- 线程与主 UI 共享进程内存，无法依靠子进程退出彻底释放 ONNX 缓存。
- `arecord` 会先录完整时长，之后才进行 VAD，不是 100 ms 音频块流式判断。
- 无法在 700 ms 连续静音或 2 秒无人声时即时结束录音。
- 生成的 `request.wav` 默认没有删除。
- 没有接入再次短按取消、音乐暂停和恢复、流式 TTS、15 FPS 限制等功能。

相关位置：

- `src/deskcamdio/services/voice.py:262`
- `src/deskcamdio/services/voice.py:281`
- `src/deskcamdio/services/voice.py:295`

### P2：设置页未覆盖设备设置需求

设置页目前只有“外观、声音、设备、数据”四个标签，设备页主要显示系统信息，没有提供以下功能：

- Wi-Fi 查看、连接与断开。
- 蓝牙开关、可发现状态、配对和已连接设备。
- 息屏时间。
- 屏幕亮度。
- 系统音量的实际控制。
- 网络和音频状态诊断。

当前音量调整只写入数据库，没有同步到底层系统音量或 mixer。

相关位置：

- `src/deskcamdio/apps/settings/app.py:14`
- `src/deskcamdio/apps/settings/app.py:61`

### P2：中文字体和声音资源没有发布保障

字体模块会尝试从 `assets/fonts/*.ttf` 加载字体，但当前源码没有对应资源，Wheel 中也没有字体。最终会回退到 pygame 默认字体，树莓派上存在中文方块乱码风险。

声音服务同样引用包内资源，但当前构建产物中没有声音文件，快门音、闹钟音和 UI 提示音不能得到发布保障。

相关位置：

- `src/deskcamdio/ui/typography.py:132`
- `src/deskcamdio/services/audio.py`

### P2：类型和代码质量门禁没有覆盖完整源码

项目配置中的 mypy 只检查以下目录：

- `src/deskcamdio/core`
- `src/deskcamdio/services`
- `src/deskcamdio/platform`

按照该配置运行 mypy 可以通过，但对整个 `src/deskcamdio` 执行检查时出现 31 个错误，分布于 13 个文件，主要包括：

- 应用 `mount(RuntimeContext)` 与 `App.mount(AppContext)` 的参数类型不兼容。
- 部分函数缺少类型标注。
- 可选回调在没有判空时直接调用。
- 应用生命周期协议与实际上下文设计不一致。

Ruff 还发现一个测试文件 import 排序错误。

相关位置：

- `pyproject.toml:49`
- `src/deskcamdio/core/lifecycle.py`
- `tests/unit/test_cli.py:1`

## 自动检查结果

| 检查项目 | 结果 |
|---|---|
| pytest | 通过，有 1 项跳过 |
| 总分支覆盖率 | 86.27% |
| Ruff format | 通过，85 个文件格式正确 |
| Ruff lint | 失败，1 个 import 排序错误 |
| 配置范围 mypy | 通过，检查 21 个文件 |
| 完整源码 mypy | 失败，31 个错误、涉及 13 个文件 |
| Wheel 构建 | 构建成功，但缺少应用清单和资源 |
| PowerShell 部署脚本 | 解析失败，9 个语法错误 |
| 树莓派实机验收 | 本次未执行 |

## 版本状态不一致

README 标题为 DeskCamdio v1.0，但阶段进度中只有阶段 A 标记完成，阶段 B 至 F 仍全部未完成：

- B：Runtime 与 StateStore。
- C：硬件服务。
- D：UI 与应用。
- E：GBA。
- F：发布和实机矩阵。

相关位置：`README.md:17`

## 建议修复顺序

### 第一阶段：修复发布阻断问题

1. 将全部 `app.toml`、字体、声音和必要素材加入 Wheel。
2. 增加“安装 Wheel 后启动运行时”的隔离环境冒烟测试。
3. 修复 STANDBY、LAUNCHER、APP 三种前台状态的统一输入转发。
4. 修复 PowerShell 部署脚本和安装自检参数。
5. 在 systemd 中明确设置 Raspberry Pi 平台和运行目录。

### 第二阶段：打通核心功能

1. 完成 Picamera2 低分辨率预览和真实快门链路。
2. 完成缩略图更新、连续拍照命名和异常恢复。
3. 将 GBA ROM 索引、GameSession、mGBA 进程和手柄返回逻辑接入运行时。
4. 将语音按键、独立 ASR worker、取消、VAD 和 TTS 接入运行时。

### 第三阶段：完成资源生命周期重构

1. 将 TaskScope 放入应用上下文。
2. 禁止应用直接创建未注册的 task、线程和子进程。
3. 所有事件订阅必须在 dispose 时解除。
4. 增加反复进入退出应用的任务数、线程数、文件描述符和 RSS 测试。

### 第四阶段：实机验收

至少覆盖以下真实硬件链路：

- IMX708 预览、拍照、滤镜、相册缩略图。
- 触控、EC11 转动、短按和长按。
- 蓝牙音响、系统音量和音乐播放。
- Wi-Fi 断线恢复。
- 中文字体和长文本换行。
- GBA ROM、手柄和紧急返回。
- 连续 20 次语音交互后的 RSS 和 swap 增长。
- systemd 开机启动、异常重启、软关机和关机耗时。

## 发布建议

当前版本不建议覆盖树莓派上的可用版本，也不建议打正式 `v1.0.0` 标签。

完成 P0 问题后，应先构建候选版本并执行以下最小端到端链路：

`安装 Wheel → systemd 启动 → 待机页 → 桌面 → 相机预览 → 拍照 → 相册 → 退出 → 关机`

该链路通过后，再进入 GBA、语音、蓝牙和长期内存稳定性验收。全部实机验收通过后，才适合发布 v1.0。
